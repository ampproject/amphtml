

describes.realWin('amp-skimlinks', {
  amp: {
    extensions: ['amp-skimlinks'],
  },
}, env => {
  // beforeEach(() => {

  // });

  it('Runs one test', () => {
    expect(true).to.be.true;
  });
});
