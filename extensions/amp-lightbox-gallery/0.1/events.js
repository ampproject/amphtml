/** @const @enum {string} */
export const LIGHTBOX_GALLERY_EVENTS_ENUM = {
  // Triggered when the lightbox attribute is newly set on an item in order to
  // process.
  NEWLY_SET: 'amp-lbg:newly-set',
};
