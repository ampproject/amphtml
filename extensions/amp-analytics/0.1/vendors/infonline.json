{
  "vars": {
    "sv": "ke",
    "ap": "1"
  },
  "transport": {
    "beacon": false,
    "xhrpost": false,
    "image": true
  },
  "requests": {
    "pageview": "${url}?st=${st}&sv=${sv}&ap=${ap}&co=${co}&cp=${cp}&ps=${ps}&host=${canonicalHost}&path=${canonicalPath}"
  },
  "triggers": {
    "pageview": {
      "on": "visible",
      "request": "pageview",
      "iframePing": true
    }
  }
}