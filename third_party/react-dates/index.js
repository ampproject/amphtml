import 'react-dates/initialize';
import * as ReactDatesConstants from 'react-dates/constants';
import * as ReactDates from 'react-dates';

window.ReactDatesConstants = ReactDatesConstants;

window.ReactDates = ReactDates;
