import {TreeProto} from './ast';

/*
 * Returns a new AST created by walking the entire tree and rendering each
 * eligible AMP Component.
 */
export function renderAmpAst(ast: TreeProto): TreeProto {
  throw new Error('TODO');
}
