language: node_js
sudo: required  # See http://docs.travis-ci.com/user/trusty-ci-environment/
dist: trusty
node_js:
  - "lts/*"
python:
  - "2.7"
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH="$HOME/.yarn/bin:$HOME/.config/yarn/global/node_modules/.bin:$PATH"
stages:
  - build
  - test
jobs:
  include:
    - stage: build
      name: "Build"
      script:
        - echo "build stage, build shard"
    - stage: build
      name: "Checks"
      script:
        - echo "build stage, checks shard"
    - stage: test
      name: "Dist, Bundle Size Tests"
      script:
        - echo "test stage, dist shard"
    - stage: test
      name: "Visual Diff Tests"
      script:
        - echo "test stage, visual diff shard"        
    - stage: test
      name: "Local Tests"
      script:
        - echo "test stage, local shard"
    - stage: test
      name: "Remote (Sauce Labs) Tests"
      script:
        - echo "test stage, remote shard"
    - stage: optional
      name: "Single Pass"
      script:
        -echo "optional stage, single pass shard"
    - stage: optional
      name: "Validator"        
      script:
        -echo "optional stage, validator shard"

