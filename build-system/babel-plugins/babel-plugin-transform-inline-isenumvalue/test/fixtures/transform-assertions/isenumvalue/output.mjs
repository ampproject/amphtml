import { exportedEnum } from './exported.mjs';
subject === "A" || subject === "B" || subject === "C";
const enumInFile = {
  FOO: 1,
  BAR: 100,
  BAZ: 2
};
x === 1 || x === 100 || x === 2;
y === "FOO" || y === 100 || y === 2;
foo === "single-item";
ignoreMe(enumInFile, x);
